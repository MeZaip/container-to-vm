# container-to-vm
A tool for converting Docker/OCI container filesystems and configuration into standalone, bootable virtual machines. Supports VM disk formats such as QCOW2 and aims to provide OVA/OVF export, allowing containerized applications to run directly inside a Linux VM without Docker.
